model products {
  id          Int     @id @default(autoincrement())
  title       String  @db.VarChar(50)
  desc        String? @db.VarChar(250)
  price       Int     @default(0)
  category_id Int
}
